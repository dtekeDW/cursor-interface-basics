# Vue Patterns Skill

- Use `<script setup lang="ts">`
- Extract stateful/shared logic into `useXxx` composables
- Keep templates declarative and focused on rendering
- Prefer small components with single responsibility
- Use descriptive state names: `isLoading`, `hasError`, `canSubmit`
