# Nuxt Server API Skill

- Place server operations in `server/api`
- Keep private logic and secrets on the server
- Validate request payloads and handle errors explicitly
- Return predictable response shapes
