# Nuxt Data Fetching Skill

- Use `useFetch` for SSR-aware route data and reactive URL-driven updates
- Use `useAsyncData` for composed/multi-source fetch logic
- Use `$fetch` for event-driven client requests
- Use `lazy: true` for non-critical data
- Use `server: false` when client-only behavior is intended
